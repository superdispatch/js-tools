{
  "name": "js-tools",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "prebump": "yarn lint && yarn test",
    "bump": "lerna version",
    "lint": "js-tools lint",
    "tdd": "jest --watch -u",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "execa": "^3.2.0",
    "husky": "^3.0.1",
    "jest": "^24.8.0",
    "lerna": "^3.16.4",
    "lint-staged": "^9.2.1",
    "prettier": "^1.18.2",
    "snapshot-diff": "^0.5.2",
    "typescript": "^3.5.3"
  }
}
