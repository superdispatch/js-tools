{
  "private": true,
  "license": "MIT",
  "scripts": {
    "tdd": "jest --watch",
    "test": "jest --coverage",
    "lint": "prettier -l \"packages/**/*.{js,json}\"",
    "lint:fix": "prettier --write  \"packages/**/*.{js,json}\""
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "eslint": "^6.1.0",
    "jest": "^24.8.0",
    "lerna": "^3.16.4",
    "prettier": "^1.18.2",
    "snapshot-diff": "^0.5.2"
  }
}
